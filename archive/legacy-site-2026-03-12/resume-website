import React, { useMemo, useState, useEffect } from "react";
import { Mail, Phone, MapPin, Github, Linkedin, Globe, Download, ExternalLink, Briefcase, GraduationCap, Award, Code2, Sun, Moon } from "lucide-react";

// ============================
// ✏️ EDIT YOUR INFO HERE
// ============================
const profile = {
  name: "Ali Ahmed",
  headline: "Founder & AI Product Lead",
  summary:
    "Founder building AI products for transit operations (BusAI): safety & maintenance logging, asset analytics, and AI-assisted dashboards for large agencies (e.g., WMATA/MTA). Mixing product, data, and design to ship reliable, human-centered tools.",
  location: "Washington, D.C.",
  email: "ali@example.com",
  phone: "+1 (555) 555‑5555",
  website: "https://busai.example",
  github: "https://github.com/alias",
  linkedin: "https://www.linkedin.com/in/ali",
  skills: [
    "Product Strategy",
    "AI/ML (LLMs, RAG)",
    "React / TypeScript",
    "Node / Express",
    "SQL / Postgres",
    "Data Viz",
    "TailwindCSS",
    "Prompt Engineering",
    "Systems Thinking",
    "Stakeholder Management",
  ],
  tools: ["AWS", "Vercel", "Supabase", "Docker", "GIS", "Framer", "Figma"],
  languages: ["English (native)", "Arabic (conversational)"]
};

const experience = [
  {
    company: "BusAI (Sideway)",
    role: "Founder & Product Lead",
    start: "2023",
    end: "Present",
    points: [
      "Shipped AI assistant for bus drivers: chat-based incident & maintenance logging with secure reporting to management.",
      "Designed SQL-driven dashboards for depot managers: fleet uptime, labor hours, incident resolution SLAs.",
      "Led enterprise pilots with transit agencies; aligned with FTA reporting standards and WSP frameworks.",
    ],
    link: "#"
  },
  {
    company: "AI Solutions Consulting",
    role: "Independent Consultant",
    start: "2020",
    end: "2023",
    points: [
      "Delivered data automation for ops teams (ETL, KPI design, cost dashboards).",
      "Built interactive prototypes in React/Node; advised on LLM integration & workflow design.",
    ],
    link: "#"
  },
];

const projects = [
  {
    name: "Transit Depot Performance Dashboard",
    description:
      "SQL + React dashboard for organization-wide depot metrics: incidents, maintenance, labor, and fleet availability.",
    highlights: ["Live filters by Department → Fleet → Bus", "Mobile/desktop responsive", "Role-based access"],
    link: "#"
  },
  {
    name: "Driver Safety Copilot",
    description:
      "LLM-powered chat for incident/safety logging with structured exports and manager escalations.",
    highlights: ["Secure reports", "Voice notes → structured data", "Multilingual prompts"],
    link: "#"
  },
];

const education = [
  {
    school: "Your University",
    degree: "B.S. in Something Analytical",
    years: "2015 – 2019",
  },
];

const awards = [
  { title: "Pilot Grant Recipient", org: "City Transit Lab", year: "2024" },
  { title: "Innovation in Ops Analytics", org: "Tech for Cities", year: "2023" },
];

// ============================
// Helpers
// ============================
const join = (arr) => arr.filter(Boolean).join(" · ");

// Print styles: simplify layout for PDF export
const PrintStyles = () => (
  <style>{`
    @page { margin: 14mm; }
    @media print {
      .no-print { display: none !important; }
      .print-fill { box-shadow: none !important; }
      .section { break-inside: avoid; }
      html { color-scheme: light; }
    }
  `}</style>
);

const Tag = ({ children }) => (
  <span className="inline-flex items-center rounded-full border px-3 py-1 text-sm leading-none">
    {children}
  </span>
);

const SectionTitle = ({ icon: Icon, children }) => (
  <div className="flex items-center gap-2 mb-4">
    {Icon ? <Icon className="h-5 w-5" aria-hidden /> : null}
    <h2 className="text-xl font-semibold tracking-tight">{children}</h2>
  </div>
);

// ============================
// Main Component
// ============================
export default function ResumeWebsite() {
  const [dark, setDark] = useState(true);

  useEffect(() => {
    document.documentElement.classList.toggle("dark", dark);
  }, [dark]);

  const jsonLd = useMemo(
    () => ({
      "@context": "https://schema.org",
      "@type": "Person",
      name: profile.name,
      jobTitle: profile.headline,
      email: `mailto:${profile.email}`,
      telephone: profile.phone,
      url: profile.website,
      sameAs: [profile.github, profile.linkedin].filter(Boolean),
      address: { "@type": "PostalAddress", addressLocality: profile.location }
    }),
    []
  );

  return (
    <div className="min-h-dvh bg-white text-neutral-900 antialiased dark:bg-neutral-950 dark:text-neutral-100">
      <PrintStyles />
      <div className="mx-auto max-w-4xl px-4 py-8 md:py-10">
        {/* Top Bar */}
        <div className="no-print sticky top-3 z-50 mb-4 flex items-center justify-between rounded-2xl border bg-white/80 p-2 shadow-sm backdrop-blur dark:border-neutral-800 dark:bg-neutral-900/70 print-fill">
          <div className="px-2 text-sm">Resume</div>
          <div className="flex items-center gap-2">
            <a href="#experience" className="rounded-xl px-3 py-2 text-sm hover:bg-neutral-100 dark:hover:bg-neutral-800">Experience</a>
            <a href="#projects" className="rounded-xl px-3 py-2 text-sm hover:bg-neutral-100 dark:hover:bg-neutral-800">Projects</a>
            <a href="#skills" className="rounded-xl px-3 py-2 text-sm hover:bg-neutral-100 dark:hover:bg-neutral-800">Skills</a>
            <a href="#education" className="rounded-xl px-3 py-2 text-sm hover:bg-neutral-100 dark:hover:bg-neutral-800">Education</a>
            <a href="#awards" className="rounded-xl px-3 py-2 text-sm hover:bg-neutral-100 dark:hover:bg-neutral-800">Awards</a>
            <button
              onClick={() => window.print()}
              className="inline-flex items-center gap-2 rounded-xl border px-3 py-2 text-sm hover:bg-neutral-50 dark:border-neutral-800 dark:hover:bg-neutral-800"
              aria-label="Print or Save as PDF"
            >
              <Download className="h-4 w-4" />
              PDF
            </button>
            <button
              onClick={() => setDark((d) => !d)}
              className="inline-flex items-center gap-2 rounded-xl border px-3 py-2 text-sm hover:bg-neutral-50 dark:border-neutral-800 dark:hover:bg-neutral-800"
              aria-label="Toggle theme"
            >
              {dark ? <Sun className="h-4 w-4" /> : <Moon className="h-4 w-4" />}
              {dark ? "Light" : "Dark"}
            </button>
          </div>
        </div>

        {/* Header */}
        <header className="print-fill mb-6 rounded-2xl border bg-gradient-to-b from-neutral-50 to-white p-6 shadow-sm dark:border-neutral-800 dark:from-neutral-900 dark:to-neutral-900/40">
          <div className="flex flex-col items-start justify-between gap-4 md:flex-row md:items-center">
            <div>
              <h1 className="text-3xl font-bold tracking-tight md:text-4xl">{profile.name}</h1>
              <p className="mt-1 text-lg text-neutral-600 dark:text-neutral-300">{profile.headline}</p>
              <p className="mt-3 max-w-2xl leading-relaxed text-neutral-700 dark:text-neutral-300">{profile.summary}</p>
            </div>
            <div className="grid gap-1 text-sm">
              {profile.location && (
                <div className="inline-flex items-center gap-2"><MapPin className="h-4 w-4"/><span>{profile.location}</span></div>
              )}
              {profile.email && (
                <a href={`mailto:${profile.email}`} className="inline-flex items-center gap-2 hover:underline"><Mail className="h-4 w-4"/><span>{profile.email}</span></a>
              )}
              {profile.phone && (
                <a href={`tel:${profile.phone}`} className="inline-flex items-center gap-2 hover:underline"><Phone className="h-4 w-4"/><span>{profile.phone}</span></a>
              )}
              <div className="inline-flex flex-wrap items-center gap-3 pt-1 text-neutral-700 dark:text-neutral-300">
                {profile.website && (
                  <a href={profile.website} className="inline-flex items-center gap-1 hover:underline"><Globe className="h-4 w-4"/>Website</a>
                )}
                {profile.github && (
                  <a href={profile.github} className="inline-flex items-center gap-1 hover:underline"><Github className="h-4 w-4"/>GitHub</a>
                )}
                {profile.linkedin && (
                  <a href={profile.linkedin} className="inline-flex items-center gap-1 hover:underline"><Linkedin className="h-4 w-4"/>LinkedIn</a>
                )}
              </div>
            </div>
          </div>
        </header>

        {/* Body Grid */}
        <main className="grid gap-6 md:grid-cols-3">
          {/* Left Column */}
          <section className="section md:col-span-2 space-y-6">
            {/* Experience */}
            <div id="experience" className="rounded-2xl border p-6 shadow-sm dark:border-neutral-800 print-fill">
              <SectionTitle icon={Briefcase}>Experience</SectionTitle>
              <ol className="relative ms-4 border-s ps-6 dark:border-neutral-800">
                {experience.map((job, i) => (
                  <li key={i} className="mb-8">
                    <div className="absolute -start-1.5 mt-1 h-3 w-3 rounded-full bg-neutral-300 ring-2 ring-white dark:bg-neutral-700 dark:ring-neutral-950"/>
                    <div className="flex items-start justify-between gap-4">
                      <div>
                        <h3 className="text-lg font-semibold">
                          {job.role} · <span className="text-neutral-600 dark:text-neutral-300">{job.company}</span>
                        </h3>
                        <p className="text-sm text-neutral-600 dark:text-neutral-400">{join([job.start, job.end])}</p>
                      </div>
                      {job.link && (
                        <a href={job.link} className="no-print inline-flex items-center gap-1 text-sm hover:underline"><ExternalLink className="h-4 w-4"/>Site</a>
                      )}
                    </div>
                    <ul className="mt-3 list-outside list-disc space-y-1 ps-5 text-[15px] leading-relaxed text-neutral-800 dark:text-neutral-200">
                      {job.points.map((p, j) => <li key={j}>{p}</li>)}
                    </ul>
                  </li>
                ))}
              </ol>
            </div>

            {/* Projects */}
            <div id="projects" className="rounded-2xl border p-6 shadow-sm dark:border-neutral-800 print-fill">
              <SectionTitle icon={Code2}>Projects</SectionTitle>
              <div className="grid gap-4 md:grid-cols-2">
                {projects.map((p, i) => (
                  <article key={i} className="rounded-xl border p-4 hover:shadow-sm dark:border-neutral-800">
                    <div className="flex items-center justify-between gap-2">
                      <h3 className="font-semibold">{p.name}</h3>
                      {p.link && (
                        <a href={p.link} className="no-print inline-flex items-center gap-1 text-sm hover:underline"><ExternalLink className="h-4 w-4"/>Demo</a>
                      )}
                    </div>
                    <p className="mt-2 text-sm text-neutral-700 dark:text-neutral-300">{p.description}</p>
                    {p.highlights?.length ? (
                      <ul className="mt-2 list-outside list-disc ps-5 text-sm text-neutral-800 dark:text-neutral-200">
                        {p.highlights.map((h, j) => <li key={j}>{h}</li>)}
                      </ul>
                    ) : null}
                  </article>
                ))}
              </div>
            </div>
          </section>

          {/* Right Column */}
          <aside className="section space-y-6">
            {/* Skills */}
            <div id="skills" className="rounded-2xl border p-6 shadow-sm dark:border-neutral-800 print-fill">
              <SectionTitle>Skills</SectionTitle>
              <div className="flex flex-wrap gap-2">
                {profile.skills.map((s, i) => <Tag key={i}>{s}</Tag>)}
              </div>
              {profile.tools?.length ? (
                <div className="mt-4">
                  <h4 className="mb-2 font-medium">Tools</h4>
                  <div className="flex flex-wrap gap-2">
                    {profile.tools.map((t, i) => <Tag key={i}>{t}</Tag>)}
                  </div>
                </div>
              ) : null}
              {profile.languages?.length ? (
                <div className="mt-4">
                  <h4 className="mb-2 font-medium">Languages</h4>
                  <div className="flex flex-wrap gap-2">
                    {profile.languages.map((t, i) => <Tag key={i}>{t}</Tag>)}
                  </div>
                </div>
              ) : null}
            </div>

            {/* Education */}
            <div id="education" className="rounded-2xl border p-6 shadow-sm dark:border-neutral-800 print-fill">
              <SectionTitle icon={GraduationCap}>Education</SectionTitle>
              <ul className="space-y-3">
                {education.map((e, i) => (
                  <li key={i}>
                    <div className="font-medium">{e.school}</div>
                    <div className="text-sm text-neutral-700 dark:text-neutral-300">{e.degree}</div>
                    <div className="text-sm text-neutral-600 dark:text-neutral-400">{e.years}</div>
                  </li>
                ))}
              </ul>
            </div>

            {/* Awards */}
            <div id="awards" className="rounded-2xl border p-6 shadow-sm dark:border-neutral-800 print-fill">
              <SectionTitle icon={Award}>Awards</SectionTitle>
              <ul className="space-y-2 text-sm">
                {awards.map((a, i) => (
                  <li key={i} className="flex items-start gap-2">
                    <Award className="mt-0.5 h-4 w-4 shrink-0"/>
                    <span>
                      <span className="font-medium">{a.title}</span>
                      {" – "}
                      <span className="text-neutral-700 dark:text-neutral-300">{a.org}</span>
                      {a.year ? `, ${a.year}` : null}
                    </span>
                  </li>
                ))}
              </ul>
            </div>
          </aside>
        </main>

        {/* Footer */}
        <footer className="mt-8 grid gap-4 rounded-2xl border p-6 text-sm text-neutral-600 shadow-sm dark:border-neutral-800 print-fill dark:text-neutral-300">
          <div className="flex flex-wrap items-center justify-between gap-3">
            <div className="flex flex-wrap items-center gap-3">
              <a href={`mailto:${profile.email}`} className="inline-flex items-center gap-2 hover:underline"><Mail className="h-4 w-4"/>Email</a>
              <a href={`tel:${profile.phone}`} className="inline-flex items-center gap-2 hover:underline"><Phone className="h-4 w-4"/>Call</a>
              {profile.website && <a href={profile.website} className="inline-flex items-center gap-2 hover:underline"><Globe className="h-4 w-4"/>Website</a>}
              {profile.github && <a href={profile.github} className="inline-flex items-center gap-2 hover:underline"><Github className="h-4 w-4"/>GitHub</a>}
              {profile.linkedin && <a href={profile.linkedin} className="inline-flex items-center gap-2 hover:underline"><Linkedin className="h-4 w-4"/>LinkedIn</a>}
            </div>
            <button onClick={() => window.print()} className="no-print inline-flex items-center gap-2 rounded-xl border px-3 py-2 hover:bg-neutral-50 dark:border-neutral-800 dark:hover:bg-neutral-800">
              <Download className="h-4 w-4"/> Print / Save PDF
            </button>
          </div>
          <div className="text-xs text-neutral-500 dark:text-neutral-400">© {new Date().getFullYear()} {profile.name}. Built with React & Tailwind.</div>
        </footer>
      </div>

      {/* SEO structured data */}
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} />
    </div>
  );
}
